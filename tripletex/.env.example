# GCP / Vertex AI
GOOGLE_CLOUD_PROJECT=your-project-id
VERTEX_AI_LOCATION=europe-north1
GEMINI_MODEL=gemini-2.5-pro

# Tripletex sandbox (local testing only — do not commit real credentials)
# Get these from your sandbox account at tripletex.no
TRIPLETEX_SANDBOX_URL=https://<sandbox-host>/v2
TRIPLETEX_SANDBOX_TOKEN=your-session-token

# Optional: protect your /solve endpoint with a bearer token
API_KEY=
